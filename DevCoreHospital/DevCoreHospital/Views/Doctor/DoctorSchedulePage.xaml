<Page
    x:Class="DevCoreHospital.Views.Doctor.DoctorSchedulePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:DevCoreHospital.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:StringNotEmptyToVisibilityConverter x:Key="StringNotEmptyToVisibilityConverter"/>
    </Page.Resources>

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Doctor Schedule"
                       FontSize="52"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1" Content="Previous" Margin="8,0,0,0" Command="{Binding PreviousDayCommand}"/>
            <Button Grid.Column="2" Content="Today" Margin="8,0,0,0" Command="{Binding TodayCommand}"/>
            <Button Grid.Column="3" Content="Next" Margin="8,0,0,0" Command="{Binding NextDayCommand}"/>

            <TextBlock Grid.Column="4"
                       Text="{Binding SelectedDateText}"
                       Margin="20,0,0,0"
                       VerticalAlignment="Center"
                       FontSize="18"/>

            <ComboBox Grid.Column="5"
                      Width="240"
                      Margin="20,0,0,0"
                      ItemsSource="{Binding Doctors}"
                      SelectedItem="{Binding SelectedDoctor, Mode=TwoWay}"
                      DisplayMemberPath="DoctorName"
                      PlaceholderText="Select doctor"/>

            <Button Grid.Column="6" Content="Refresh" Margin="12,0,0,0" Command="{Binding RefreshCommand}"/>
        </Grid>

        <!-- Error -->
        <Border Grid.Row="1"
                Background="#FFF7ED"
                BorderBrush="#F59E0B"
                BorderThickness="1"
                CornerRadius="14"
                Padding="14,10"
                Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#9A3412" TextWrapping="Wrap"/>
        </Border>

        <!-- Appointments -->
        <Grid Grid.Row="2">
            <!-- Empty state -->
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="8"
                        Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="No appointments for this day."
                           FontSize="20"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Try Previous/Next or Refresh."
                           Foreground="#9CA3AF"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- List -->
            <ListView ItemsSource="{Binding Appointments}" SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,12"/>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#111827"
                                CornerRadius="14"
                                Padding="16">
                            <Grid ColumnSpacing="14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Spacing="6">
                                    <TextBlock Text="{Binding PatientName}" FontSize="20" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding DateText}" Foreground="#9CA3AF"/>
                                    <TextBlock Text="{Binding Notes}" TextWrapping="WrapWholeWords" Foreground="#D1D5DB"/>
                                </StackPanel>

                                <Button Grid.Column="1"
                                        Content="Details"
                                        Click="DetailsButton_Click"
                                        VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>