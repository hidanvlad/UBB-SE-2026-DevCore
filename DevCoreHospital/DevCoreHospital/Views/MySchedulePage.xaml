<Page
    x:Class="DevCoreHospital.Views.MySchedulePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Page.Resources>
		<SolidColorBrush x:Key="BgBrush" Color="#FF0B120D"/>
		<SolidColorBrush x:Key="CardBrush" Color="#FF0F1711"/>
		<SolidColorBrush x:Key="TextBrush" Color="#FFFFFFFF"/>
		<SolidColorBrush x:Key="MutedBrush" Color="#FFB7C2BA"/>
		<SolidColorBrush x:Key="AccentBrush" Color="#FF166534"/>
	</Page.Resources>

	<Grid Background="{StaticResource BgBrush}" Padding="24">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="2*"/>
			<ColumnDefinition Width="16"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<!-- Left: Doctor selector + Future shifts -->
		<Border Grid.Column="0"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource AccentBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="16">
			<StackPanel Spacing="10">
				<TextBlock Text="My Future Shifts"
                           Foreground="{StaticResource TextBrush}"
                           FontSize="24"
                           FontWeight="SemiBold"/>

				<TextBlock Text="Select doctor (test mode)"
                           Foreground="{StaticResource MutedBrush}"/>

				<ComboBox ItemsSource="{Binding Doctors}"
                          SelectedItem="{Binding SelectedDoctor, Mode=TwoWay}"
                          DisplayMemberPath="DisplayName"
                          PlaceholderText="Select doctor"/>

				<ListView ItemsSource="{Binding FutureShifts}"
                          SelectedItem="{Binding SelectedShift, Mode=TwoWay}"
                          Background="Transparent"
                          MinHeight="300">
					<ListView.ItemTemplate>
						<DataTemplate>
							<Border BorderBrush="{StaticResource AccentBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="10"
                                    Margin="0,0,0,8">
								<StackPanel>
									<TextBlock Text="{Binding DateText}" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold"/>
									<TextBlock Text="{Binding TimeRangeText}" Foreground="{StaticResource MutedBrush}"/>
									<TextBlock Text="{Binding LocationText}" Foreground="{StaticResource MutedBrush}"/>
									<TextBlock Text="{Binding Status}" Foreground="{StaticResource MutedBrush}"/>
								</StackPanel>
							</Border>
						</DataTemplate>
					</ListView.ItemTemplate>
				</ListView>
			</StackPanel>
		</Border>

		<!-- Right: Request swap -->
		<Border Grid.Column="2"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource AccentBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="16">
			<StackPanel Spacing="12">
				<TextBlock Text="Request Shift Swap"
                           Foreground="{StaticResource TextBrush}"
                           FontSize="24"
                           FontWeight="SemiBold"/>

				<TextBlock Text="Choose colleague (same role/profile)"
                           Foreground="{StaticResource MutedBrush}"/>

				<ComboBox ItemsSource="{Binding EligibleColleagues}"
                          SelectedItem="{Binding SelectedColleague, Mode=TwoWay}"
                          DisplayMemberPath="DisplayName"
                          PlaceholderText="Select colleague"/>

				<Button Content="Request Swap"
                        Command="{Binding RequestSwapCommand}"
                        Background="{StaticResource AccentBrush}"
                        Foreground="White"
                        HorizontalAlignment="Stretch"
                        Padding="10"/>

				<InfoBar IsOpen="True"
                         IsClosable="False"
                         Severity="Informational"
                         Message="{Binding StatusMessage}" />
			</StackPanel>
		</Border>
	</Grid>
</Page>